import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  resultContainer: {
    alignItems: 'center',
    marginVertical: 20,
  },
  choiceText: {
    fontSize: 18,
    color: '#666',
    marginVertical: 5,
    textTransform: 'capitalize',
  },
});
