import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  resultContainer: {
    alignItems: 'center',
    padding: 10,
  },
  resultText: {
    fontSize: 24,
    fontWeight: 'bold',
    textAlign: 'center',
  },
});
