import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  balanceContainer: {
    alignItems: 'center',
  },
  balanceLabel: {
    fontSize: 12,
    color: '#fff',
    opacity: 0.8,
  },
  balanceAmount: {
    fontSize: 18,
    fontWeight: 'bold',
    color: '#fff',
  },
});
