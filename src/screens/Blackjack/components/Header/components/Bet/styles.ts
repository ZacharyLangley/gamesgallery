import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  betContainer: {
    alignItems: 'center',
  },
  betLabel: {
    fontSize: 12,
    color: '#fff',
    opacity: 0.8,
  },
  betAmount: {
    fontSize: 18,
    fontWeight: 'bold',
    color: '#FFD700',
  },
});
