import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    width: '100%',
    padding: 20,
  },
  winContainer: {
    alignItems: 'center',
  },
  winIcon: {
    marginBottom: 20,
  },
  attemptsText: {
    fontSize: 18,
    color: '#666',
  },
});
